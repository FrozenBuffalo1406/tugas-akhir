<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.main.DashboardActivity">

    <!-- Toolbar buatan kita (bukan Action Bar) -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.ECGApp.AppBarOverlay"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary">

            <TextView
                android:id="@+id/tvDashboardTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:text="@string/dashboard"
                android:textAppearance="@style/TextAppearance.AppCompat.Title"
                android:textColor="@color/white"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <ImageView
                android:id="@+id/ivProfileIcon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginEnd="16dp"
                android:src="@drawable/ic_profile_placeholder"
                android:contentDescription="@string/profile"
                android:padding="4dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/appBarLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:paddingBottom="100dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_dashboard" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- FAB 3: Provisioning BLE (Baru) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabBle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:clickable="false"
        android:contentDescription="@string/provisioning_wifi"
        android:src="@drawable/ic_bluetooth"
        android:visibility="gone"
        app:fabSize="mini"
        app:layout_constraintBottom_toTopOf="@+id/fabQr"
        app:layout_constraintEnd_toEndOf="@+id/fabScan"
        app:tint="@android:color/white"
        tools:ignore="ClassNameCheck"
        tools:visibility="visible" />
    <TextView
        android:id="@+id/labelFabBle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/provisioning_wifi"
        android:padding="4dp"
        android:background="@drawable/label_background"
        android:textColor="@android:color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/fabBle"
        app:layout_constraintTop_toTopOf="@id/fabBle"
        app:layout_constraintEnd_toStartOf="@id/fabBle"
        android:layout_marginEnd="8dp"
        tools:visibility="visible"/>


    <!-- FAB 2: Scan QR (Lama, tapi jadi mini) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabQr"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:clickable="false"
        android:contentDescription="@string/scan_qr_code"
        android:src="@drawable/ic_qr_code"
        android:visibility="gone"
        app:fabSize="mini"
        app:layout_constraintBottom_toTopOf="@+id/fabScan"
        app:layout_constraintEnd_toEndOf="@+id/fabScan"
        app:tint="@android:color/white"
        tools:ignore="ClassNameCheck"
        tools:visibility="visible" />
    <TextView
        android:id="@+id/labelFabQr"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/scan_qr"
        android:padding="4dp"
            android:background="@drawable/label_background"
        android:textColor="@android:color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/fabQr"
        app:layout_constraintTop_toTopOf="@id/fabQr"
        app:layout_constraintEnd_toStartOf="@id/fabQr"
        android:layout_marginEnd="8dp"
        tools:visibility="visible"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabScan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="@string/tambah_aksi"
        android:src="@android:drawable/ic_input_add"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@android:color/white"
        tools:ignore="ClassNameCheck" />

</androidx.constraintlayout.widget.ConstraintLayout>