<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/ecg_app_native"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECGApp">

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ECGApp.NoActionBar"> <!-- Tambahin tema di sini juga biar aman -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:theme="@style/Theme.ECGApp.NoActionBar"/>

        <!-- Dashboard & Fitur Utama -->
        <activity android:name=".ui.main.DashboardActivity"
            android:label="Dashboard"
            android:theme="@style/Theme.ECGApp.NoActionBar" />

        <activity android:name=".ui.profile.ProfileActivity"
            android:label="Profil Saya"
            android:theme="@style/Theme.ECGApp.NoActionBar" />

        <activity android:name=".ui.history.HistoryActivity"
            android:label="Riwayat Bacaan"
            android:theme="@style/Theme.ECGApp.NoActionBar" />

        <activity
            android:name=".ui.history.detail.HistoryDetailActivity"
            android:label="Detail Bacaan"
            android:theme="@style/Theme.ECGApp.NoActionBar" />

        <activity
            android:name=".ui.ble.BleProvisioningActivity"
            android:label="Provisioning"
            android:theme="@style/Theme.ECGApp.NoActionBar" />
        <activity
            android:name=".ui.ble.BlePairingActivity"
            android:label="Pairing Device"
            android:theme="@style/Theme.ECGApp.NoActionBar" />

        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="android:screenOrientation"
            tools:ignore="DiscouragedApi" />
    </application>
</manifest>